# Python - Exception Handling

This project introduces exception handling in Python. It teaches how to use `try` and `except` to prevent a program from crashing when an error happens, such as accessing an invalid list index.