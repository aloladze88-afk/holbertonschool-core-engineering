# Python - Control Flow

This project introduces the basic control flow tools used in Python. Control flow allows a programme to make decisions, repeat actions, and choose different instructions depending on specific conditions.

The project focuses on conditional statements such as `if`, `elif`, and `else`, comparison operators, Boolean logic, `while` loops, and `for` loops with `range()`.